<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Buttons" parent="@style/Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="android:stateListAnimator">@null</item>
        <item name="android:layout_width">88dp</item>
        <item name="android:layout_height">88dp</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/baseText</item>
        <item name="minWidth">88dp</item>
        <item name="maxHeight">88dp</item>
        <item name="android:padding">@dimen/base</item>
    </style>
</resources>